# Slim dependency set for the HTTP API image — just what backend/app +
# src/tools.py (and its transitive src/ imports) actually need at runtime.
# The repo-root requirements.txt is the superset used for development, the
# MCP server, the agents, the evals, and the tests.

fastapi>=0.110.0
uvicorn[standard]>=0.29.0
pydantic>=2.6.0
pydantic-settings>=2.2.0
httpx>=0.27.0
