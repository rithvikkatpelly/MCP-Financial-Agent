name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.11", "3.12"]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
      - run: pip install -r requirements.txt
      - name: Lint
        run: ruff check .
      - name: Unit + integration tests
        run: pytest -q
      - name: Orchestrator routing eval (prints the table)
        run: python tests/test_routing.py
      - name: Pipeline eval — worker routing / retry / degradation (prints the table)
        run: python tests/test_pipeline_eval.py
      - name: Evaluation suite (stub backend, hermetic)
        run: python -m evals
      - name: Upload eval report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: eval-report-py${{ matrix.python-version }}
          path: evals/REPORT.md
