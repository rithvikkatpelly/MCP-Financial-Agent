# Pinned to the 1.x line on purpose: mcp==2.0.0 shipped a breaking API
# change and no longer exposes mcp.server.fastmcp.FastMCP the way this
# project uses it. Re-check before bumping.
mcp==1.9.4
httpx>=0.27.0
python-dotenv>=1.0.0
pydantic>=2.6.0

# Multi-agent orchestrator's live backend (AGENT_BACKEND=anthropic).
# The default stub backend, the evals, and the tests need none of this.
anthropic>=0.40.0

# HTTP interface (backend/app) — a second, independently deployable surface
# over the same src/tools.py logic the MCP server uses. The MCP server itself
# (src/server.py) needs none of this.
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
pydantic-settings>=2.2.0

# Dev / CI
pytest>=8.0.0
ruff>=0.6.0

# Demo UI only (streamlit run streamlit_app.py). Not needed by the server,
# the agents, the evals, or the tests.
streamlit>=1.30.0
pandas>=2.0.0
